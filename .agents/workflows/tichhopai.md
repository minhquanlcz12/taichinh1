---
description: Automatically setup AI integration structure (Gemini/OpenAI).
---

1. Create `js/ai_engine.js` with standard integration boilerplate.
2. Setup structured prompt templates for common tasks (Analysis, Generation).
3. Add API key configuration placeholder in `js/db.js` or a dedicated config file.
4. Provide a sample UI component for AI interaction (e.g., an AI Chatbot or Analysis button).
