---
description: Setup advanced data reporting and visualization (Charts, Excel).
---

1. Load `Chart.js` and `SheetJS` (xlsx) libraries in `index.html`.
2. Create `js/reports.js` with boilerplate for:
- Data visualization charts (Bar, Line, Pie).
- Excel export functionality for any data array.
3. Add a "Báo cáo" (Report) section/modal to the current UI if missing.
